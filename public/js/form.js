/*
    The function formSubmit() is called when the form "myform" is submitted.
    It runs some validations and shows the output.
*/
function formSubmit(){

    // uncomment this line to bypass the validations. Will want this for client side validation, but will
    // want server side validation as well in case someone disables javascript in their browser
    return true;
}